{
  "name": "@kapoost/abzu-gui",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Abzu GUI — operator/reviewer/sponsor views over the orchestrator HTTP API",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch run src/server.ts",
    "typecheck": "bun --bun tsc --noEmit",
    "seed:post-deploy": "bun scripts/post-deploy-e2e.mjs"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.2",
    "@types/bun": "latest",
    "playwright": "^1.55.0",
    "tailwindcss": "^4.3.2",
    "typescript": "^5.6.0"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
